{
  "name": "@razors/babel-plugin",
  "private": true,
  "devDependencies": {
    "@types/jest": "^24.0.13",
    "@types/node": "^12.0.4",
    "jest": "^26.1.0",
    "lerna": "^3.14.1",
    "ts-jest": "^26.1.1",
    "typescript": "^3.9.5"
  },
  "scripts": {
    "test": "jest",
    "publish": "npm run build && npm run test && ./node_modules/.bin/lerna publish",
    "build": "./node_modules/.bin/lerna run build"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "vue": "3.0.0-beta.18"
  }
}
